[project]
name = "gc2-tools"
version = "0.1.0"
description = "GC2 Connect Unity development tools"
requires-python = ">=3.11"
dependencies = []

[project.scripts]
gc2-simulator = "gc2_simulator:main"
mock-gspro = "mock_gspro_server:main"
